

# --- Port ---
http_port 3128

# --- Safe Ports & SSL ---
acl SSL_ports port 443
acl Safe_ports port 80
acl Safe_ports port 21
acl Safe_ports port 443
acl Safe_ports port 70
acl Safe_ports port 210
acl Safe_ports port 1025-65535
acl Safe_ports port 280
acl Safe_ports port 488
acl Safe_ports port 591
acl Safe_ports port 777

http_access deny !Safe_ports
http_access deny CONNECT !SSL_ports

# --- Manager ACL ---
http_access allow localhost manager
http_access deny manager

# --- Blocked Sites ---
acl blocked_sites dstdomain .facebook.com .youtube.com .reddit.com .tiktok.com .instagram.com

# --- Allowed Sites ---
acl allowed_sites dstdomain .github.com .stackoverflow.com .google.com .wikipedia.org .whatismyipaddress.com

# --- Localhost source ACL ---
acl localnet src 0.0.0.0/0

# --- Access Rules (ORDER MATTERS) ---

http_access deny blocked_sites
http_access deny CONNECT blocked_sites  
http_access allow localnet allowed_sites
http_access deny CONNECT all               
http_access deny all

# --- Logging ---
access_log /var/log/squid/access.log squid
cache_log /var/log/squid/cache.log

# --- Cache ---
cache_dir ufs /var/spool/squid 100 16 256
coredump_dir /var/spool/squid

refresh_pattern ^ftp:           1440    20%     10080
refresh_pattern ^gopher:        1440    0%      1440
refresh_pattern -i (/cgi-bin/|\?) 0     0%      0
refresh_pattern .               0       20%     4320